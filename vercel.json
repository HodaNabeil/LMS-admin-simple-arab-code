{
  "buildCommand": "pnpm run openapi:generate && pnpm build",
  "outputDirectory": "dist",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install",
  "framework": "vite",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "dev": true
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

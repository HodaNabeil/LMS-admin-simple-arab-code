import AddPathForm from "./AddPathFrom.tsx";

export default function NewAddPath() {
  return (
    <div className="!pt-[5rem]">
      <AddPathForm onSubmit={() => {}} onCancel={() => {}} />
    </div>
  );
}
